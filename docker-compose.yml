services:
    # Laravel app service
    app:
        build:
            context: .
        container_name: laravel-app
        volumes:
            - .:/var/www/html # Mount the Laravel project directory
            - ./storage/logs:/var/log/laravel # Mount Laravel logs for Logstash
        environment:
            - APP_URL=http://localhost
            - APP_ENV=local
            - APP_DEBUG=true
            - APP_KEY=base64:d3G8x1u68ubU7h0pZH4WLbgZdW+lzp1wGXApGK+oj+s= # Use your actual APP_KEY here
            - DB_CONNECTION=mysql
            - DB_HOST=mysql # MySQL service name
            - DB_PORT=3306
            - DB_DATABASE=laravel
            - DB_USERNAME=user
            - DB_PASSWORD=password
        depends_on:
            - mysql
        networks:
            - app-network

    # Web server (Nginx)
    webserver:
        image: nginx:stable-alpine
        container_name: laravel-web
        ports:
            - "8000:80" # Map host port 8000 to container's port 80
        volumes:
            - .:/var/www/html # Mount Laravel project directory
            - ./nginx.conf:/etc/nginx/conf.d/default.conf # Nginx configuration
        depends_on:
            - app
        networks:
            - app-network

    # MySQL database service
    mysql:
        image: mysql:8.0
        container_name: mysql-db
        environment:
            MYSQL_ROOT_PASSWORD: root
            MYSQL_DATABASE: laravel
            MYSQL_USER: user
            MYSQL_PASSWORD: password
        ports:
            - "3306:3306" # Ensure MySQL is accessible on port 3306
        networks:
            - app-network

    # phpMyAdmin service
    phpmyadmin:
        image: phpmyadmin/phpmyadmin:latest
        container_name: phpmyadmin
        environment:
            - PMA_HOST=mysql # MySQL service name (phpMyAdmin connects to MySQL)
            - PMA_USER=user
            - PMA_PASSWORD=password
        ports:
            - "8080:80" # Map host port 8080 to phpMyAdmin container port 80
        networks:
            - app-network
        depends_on:
            - mysql
        restart: unless-stopped

networks:
    app-network:
        driver: bridge

server {
    listen 80;
    server_name localhost;

    root /var/www/html/public;  # This should point to the Laravel 'public' directory

    index index.html index.php index.htm;

    location / {
        try_files $uri $uri/ /index.html;  # Serve static index.html for testing
    }

    # Disable PHP processing for now
    # location ~ \.php$ {
    #     include fastcgi_params;
    #     fastcgi_pass app:9000;
    #     fastcgi_param SCRIPT_FILENAME /var/www/html/public$fastcgi_script_name;
    #     include fastcgi_params;
    # }
}
